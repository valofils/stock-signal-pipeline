[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-signal"
version = "0.1.0"
description = "End-to-end stock signal pipeline with automated daily reports"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
